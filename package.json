{
    "name": "@mc.rehab/models",
    "version": "0.0.54",
    "main": "dist/ModelsModule.js",
    "types": "dist/ModelsModule.d.ts",
    "license": "MIT",
    "preversion": "rm -rf dist && tsc",
    "postversion": "npm publish",
    "scripts": {
        "start:dev": "concurrently --handle-input \"wait-on dist/main.js && nodemon\" \"tsc -w -p tsconfig.json\" ",
        "release": "rm -rf dist && tsc && npm version patch && npm publish --access=private",
        "build": "tsc"
    },
    "dependencies": {
        "@nestjs.pro/common": "^1.0.115",
        "@nestjs/common": "^7.1.3",
        "@nestjs/core": "^7.1.3",
        "@nestjs/swagger": "^4.5.10",
        "@nestjs/typeorm": "^7.1.0",
        "bcrypt": "^5.0.0",
        "class-transformer": "^0.2.3",
        "class-validator": "^0.12.2",
        "mysql": "^2.18.1",
        "typeorm": "^0.2.25",
        "typescript": "^4.3.2"
    },
    "devDependencies": {
        "@types/node": "^14.0.13",
        "concurrently": "^5.2.0",
        "wait-on": "^5.0.1"
    }
}
